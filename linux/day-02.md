## Day 02 – File Operations

Topics:
- cp and mv commands
- Recursive directory copy
- Wildcards (* ? [])
- Tab completion

Practice:
- Copied and moved files between directories
- Used wildcards for bulk operations
- rmdir dir → removes empty directory
- rmdir -p a/b/c → removes empty parent directories
- Works only if directory is empty

